{
  "name": "campfire-cove-bot",
  "version": "2.0.0",
  "private": true,
  "description": "The all-in-one Discord bot for Campfire Cove",
  "main": "index.js",
  "scripts": {
    "start": "node deploy-commands.js && node index.js",
    "deploy": "node deploy-commands.js",
    "check": "node --check index.js && node --check deploy-commands.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "discord.js": "^14.22.1",
    "dotenv": "^16.6.1"
  }
}
